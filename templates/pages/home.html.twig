{% extends 'base.html.twig' %}

{% block title %}
    Quai Antique - Bienvenue !
{% endblock %}

{% block body %}

{% if not plats.items is same as([]) %}

<div class="container mt-4">
    {# flash info success a la modification du profil utilisateur #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}
    <div class="jumbotron">
        <h1 class="display-4 text-center text-warning">Bienvenue au Quai Antique!</h1>
        <p class="lead text-center">Venez deguster nos delicieux produit issu de notre belle region La Savoie ! </p>
        <hr class="my-4">
    </div>

    <div class="row">
        {% for plat in plats %}
        <div class="col-lg-3">
            <div class="card mb-3">
                <img class="fit-picture" src=" {{plat.image}} " alt="image de plats incontournable">
                <span class="a-txt"> {{plat.title}} </span>
            </div>
        </div>
        {% endfor %}
    </div>
    <div class="navigation d-flex justify-content-center mt-4">
        {{ knp_pagination_render(plats) }}
    </div>
    <div class="m-4">
        <button class="btn btn-primary btn-lg"; type="button">Réserver une table</button>
    </div>

    {% else %}
        <h4 class="m-4">Il n'y a pas de plats</h4>
    {% endif %}
</div>
<style>
    .active1 {
        color:orange;
    }
    .card {
        display: grid;
    }
    .card>*{
    grid-area:1/1/-1/-1; 
    /* raccourci pour grid-row et grid-column */
    }
    .a-txt{
    display:flex;
    justify-content:center;
    align-items:center;
    color:#fff;
    opacity:0;
    transition:opacity .8s;
    }
    .a-txt:hover{
    opacity:1;
    background:#999999;
    }
    img{
    width:100%;
    height:auto;
    border-radius:4px
    }
    a{
    text-decoration:none;
    }
</style>

{% endblock %}

